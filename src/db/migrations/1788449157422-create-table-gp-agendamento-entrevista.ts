import { MigrationInterface, QueryRunner } from 'typeorm'

export class CreateTableGpAgendamentoEntrevista1788449157422 implements MigrationInterface {
  public async up(queryRunner: QueryRunner): Promise<void> {
    await queryRunner.query(`
      CREATE TABLE GP_AGENDAMENTO_ENTREVISTA (
        CODIGO NUMBER
          GENERATED BY DEFAULT ON NULL AS IDENTITY
          CONSTRAINT PK_GP_AGENDAMENTO_ENTREVISTA PRIMARY KEY,
        CODIGO_CANDIDATURA NUMBER NOT NULL,
        DATA_ENTREVISTA DATE NOT NULL,
        DURACAO_MINUTOS NUMBER,
        HORA_FIM VARCHAR2(5),
        CODIGO_MODALIDADE NUMBER NOT NULL,
        LOCAL VARCHAR2(255),
        LINK VARCHAR2(500),
        OBSERVACAO VARCHAR2(1000),
        JUSTIFICATIVA VARCHAR2(1000),
        ESTADO NUMBER NOT NULL,
        CRIADO_POR NUMBER,
        CRIADO_EM DATE DEFAULT SYSDATE NOT NULL,
        ATUALIZADO_EM DATE DEFAULT SYSDATE,
        CONSTRAINT FK_GP_AGEND_ENTR_CANDIDATURA
          FOREIGN KEY (CODIGO_CANDIDATURA)
          REFERENCES GP_CANDIDATURAS (CODIGO),
        CONSTRAINT FK_GP_AGEND_ENTR_MODALIDADE
          FOREIGN KEY (CODIGO_MODALIDADE)
          REFERENCES GP_MODALIDADE (CODIGO),
        CONSTRAINT FK_GP_AGEND_ENTR_ESTADO
          FOREIGN KEY (ESTADO)
          REFERENCES GP_ESTADO_AGENDAMENTO_ENTREVISTA (CODIGO),
        CONSTRAINT FK_GP_AGEND_ENTR_CRIADO_POR
          FOREIGN KEY (CRIADO_POR)
          REFERENCES GP_USUARIOS (CODIGO)
      )
    `)
  }

  public async down(queryRunner: QueryRunner): Promise<void> {
    await queryRunner.query(`
      DROP TABLE GP_AGENDAMENTO_ENTREVISTA CASCADE CONSTRAINTS
    `)
  }
}
