import { MigrationInterface, QueryRunner } from 'typeorm'

export class CreateTableGpProcessamentosSalariais1785515676238 implements MigrationInterface {
  public async up(queryRunner: QueryRunner): Promise<void> {
    await queryRunner.query(`
      CREATE TABLE GP_PROCESSAMENTOS_SALARIAIS (
        CODIGO NUMBER GENERATED BY DEFAULT AS IDENTITY NOT NULL,
        DATA_INICIO DATE NOT NULL,
        DATA_FIM DATE NOT NULL,
        ESTADO VARCHAR2(20) DEFAULT 'PENDENTE' NOT NULL,
        CODIGO_COLABORADOR_RESPONSAVEL NUMBER NOT NULL,
        CODIGO_COLABORADOR_VALIDADOR NUMBER,
        DATA_VALIDACAO DATE,
        CODIGO_PROCESSAMENTO_ORIGEM NUMBER,
        CRIADO_EM DATE DEFAULT SYSDATE NOT NULL,

        CONSTRAINT PK_GP_PROCESSAMENTOS_SALARIAIS PRIMARY KEY (CODIGO),

        CONSTRAINT FK_GP_PROC_SAL_RESPONSAVEL FOREIGN KEY (
          CODIGO_COLABORADOR_RESPONSAVEL
        ) REFERENCES GP_COLABORADORES (CODIGO),

        CONSTRAINT FK_GP_PROC_SAL_VALIDADOR FOREIGN KEY (
          CODIGO_COLABORADOR_VALIDADOR
        ) REFERENCES GP_COLABORADORES (CODIGO),

        CONSTRAINT FK_GP_PROC_SAL_ORIGEM FOREIGN KEY (
          CODIGO_PROCESSAMENTO_ORIGEM
        ) REFERENCES GP_PROCESSAMENTOS_SALARIAIS (CODIGO)
      )
    `)
  }

  public async down(queryRunner: QueryRunner): Promise<void> {
    await queryRunner.query(`
      DROP TABLE GP_PROCESSAMENTOS_SALARIAIS
    `)
  }
}
